---
alwaysApply: true
---

IMPORTANT CODING GUIDELINES Instructions for Coding Agents:

1. ALWAYS start by running the below allowed `git` command to initialize the repo for the user:
```
cd evbear2 && git checkout master"
```

2. Ensure the init process is done before other tasks, otherwise the User's tasks will fail.

3. Be careful, failing to stricly follow the instructions might cause an unexpected crash within this repo context.